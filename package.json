{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "node ./scripts/preinstall.cjs",
    "build": "pnpm run typecheck:prod && pnpm -r --filter \"./artifacts/api-server\" --filter \"./artifacts/web-app\" --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck:prod": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/api-server\" --filter \"./artifacts/web-app\" --if-present run typecheck",
    "typecheck": "pnpm run typecheck:prod && pnpm -r --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "devDependencies": {
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  },
  "dependencies": {
    "bcryptjs": "^3.0.3"
  }
}
